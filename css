/* Basic reset for margin and padding */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Set the background and some padding for the page */
body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    padding: 20px;
}

/* Styling for the container */
.container {
    max-width: 600px;
    margin: 0 auto;
    background-color: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* Header styling */
h1 {
    text-align: center;
    color: #333;
}

/* Styling for the list of greetings */
#greetings-list {
    list-style-type: none;
    padding: 0;
}

#greetings-list li {
    padding: 10px;
    font-size: 18px;
    margin-bottom: 10px;
    border: 1px solid #ddd;
    border-radius: 4px;
    transition: background-color 0.3s ease;
}

#greetings-list li.hello {
    background-color: #a8e6cf;
    color: #3f3f3f;
}

#greetings-list li.goodbye {
    background-color: #ff8c94;
    color: #3f3f3f;
}

#message {
    font-size: 16px;
    font-weight: bold;
    margin-top: 20px;
}
